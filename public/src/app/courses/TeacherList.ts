export class TeacherList{
  teacherId: number;
  teacherName: string

  constructor(teacherId: number, teacherName: string) {
    this.teacherId = teacherId;
    this.teacherName = teacherName;
  }
}
